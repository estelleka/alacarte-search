{
  "name": "alacarte-search",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "serve": "^14.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "search-insights": "^2.17.3"
  }
}
